version: "3.9"

services:
  api:
    build: .
    image: bidding-api:dev
    ports:
      - "8080:8080"
    environment:
      - PORT=8080
      - DATABASE_URL=sqlite:///data/dev.db
    volumes:
      - ./data:/data
    restart: unless-stopped
